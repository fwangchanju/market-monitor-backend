name: Release

on:
  push:
    branches:
      - 'release/**'
  workflow_dispatch:
    inputs:
      target:
        description: '빌드 대상'
        required: true
        default: 'application'
        type: choice
        options:
          - application
          - nginx
          - renderer
          - all

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push application image
        if: github.event_name == 'push' || inputs.target == 'application' || inputs.target == 'all'
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile
          push: true
          tags: ghcr.io/fwangchanju/market-monitor:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build and push nginx image
        if: github.event_name == 'push' || inputs.target == 'nginx' || inputs.target == 'all'
        uses: docker/build-push-action@v6
        with:
          context: .
          file: containers/nginx/Dockerfile
          push: true
          tags: ghcr.io/fwangchanju/market-monitor-nginx:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build and push renderer image
        if: github.event_name == 'push' || inputs.target == 'renderer' || inputs.target == 'all'
        uses: docker/build-push-action@v6
        with:
          context: .
          file: containers/renderer/Dockerfile
          push: true
          tags: ghcr.io/fwangchanju/market-monitor-renderer:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Deploy to server 1 (application/nginx)
        if: github.event_name == 'push' || inputs.target == 'application' || inputs.target == 'nginx' || inputs.target == 'all'
        uses: appleboy/ssh-action@v1
        env:
          CR_PAT: ${{ secrets.CR_PAT }}
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          envs: CR_PAT
          script: |
            DEPLOY_DIR=~/repo/market-monitor-backend/infra/scripts
            TARGET="${{ inputs.target }}"
            EVENT="${{ github.event_name }}"

            if [ "$EVENT" = "push" ] || [ "$TARGET" = "all" ]; then
              bash "$DEPLOY_DIR/deploy.sh"
            elif [ "$TARGET" = "application" ]; then
              bash "$DEPLOY_DIR/deploy-application.sh"
            elif [ "$TARGET" = "nginx" ]; then
              bash "$DEPLOY_DIR/deploy-nginx.sh"
            fi

      - name: Deploy to server 2 (renderer)
        if: github.event_name == 'push' || inputs.target == 'renderer' || inputs.target == 'all'
        uses: appleboy/ssh-action@v1
        env:
          CR_PAT: ${{ secrets.CR_PAT }}
        with:
          host: ${{ secrets.RENDERER_SERVER_HOST }}
          username: ${{ secrets.RENDERER_SERVER_USER }}
          key: ${{ secrets.RENDERER_SERVER_SSH_KEY }}
          envs: CR_PAT
          script: |
            DEPLOY_DIR=~/repo/market-monitor-backend/infra/scripts
            bash "$DEPLOY_DIR/deploy-renderer.sh"
