# Codex 작업 규칙

이 저장소에서는 `CLAUDE.md`와 `docs/rules/process.md`의 작업 방식도 따른다. 사용자와는 한국어로 대화한다.

## 실행 권한

- PR 병합과 GitHub Actions 워크플로 수동 실행은 하지 않는다. 셸, GitHub MCP, 앱 도구 등 경로와 관계없이 동일하다.
- `infra/.env`와 다른 `.env` 파일의 내용을 읽거나 수정하거나 출력하지 않는다. 필요한 키는 변수 이름만 참조하고, 실행 환경에 주입된 값을 사용한다.
- `git commit`, `git push`, PR 생성·수정은 실행 전에 사용자의 명시적 승인을 받는다. 승인받은 범위만 실행한다.
- DB 데이터 삭제·마이그레이션 실행, force push 등 되돌리기 어려운 작업은 실행 전에 사용자의 명시적 승인을 받는다. 지시서와 검증 쿼리 작성은 이 승인 없이 진행한다.
- 권한 규칙을 우회하기 위해 명령을 다른 셸, 스크립트, 도구 또는 에이전트로 감싸지 않는다.
