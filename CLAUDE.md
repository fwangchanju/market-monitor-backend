# CLAUDE.md

## 문서

작업 전 아래를 참고한다.

**지켜야 하는 것**

- 작업 방식·PR·완료 기준: `docs/rules/process.md`
- 코드 스타일: `docs/rules/style.md`
- 테스트 기준: `docs/rules/testing.md`
- 커밋 컨벤션: `docs/rules/commit.md`

**알아야 하는 것**

- 구조·패키지 판단 기준: `docs/architecture.md` — 새 파일을 어디에 둘지 판단할 때 먼저 본다
- 배포·롤백·장애 대응: `docs/operations.md` — 배포나 롤백이 필요하면 코드부터 뒤지지 말고 여기부터 본다
- 왜 이렇게 만들었나: `docs/decisions.md` — 이미 논의해서 결론이 난 사안이 적혀 있다. 같은 판단을 다시 하기 전에 본다

`docs/decisions.md`는 **직접 수정하지 않는다.** 작업 중 알게 된 것은 PR 설명에 남긴다.

## 대화

사용자와의 모든 대화는 한국어로 진행한다.

## 작업

작업은 PR 단위로 자율 진행한다. 작업 하나를 통으로 끝내고 PR을 올린 뒤 멈춘다. 중간에 세부 판단이
필요하면 스스로 결정하고, 그 결정과 이유를 PR 설명에 남긴다. 자세한 규칙은 `docs/rules/process.md`.

**PR 병합은 어떤 경우에도 직접 하지 않는다.** 사용자가 병합한다.

되돌리기 어려운 작업(데이터 삭제, DB 마이그레이션, force push)은 실행 전에 사용자에게 확인한다.

PR 생성 후 리뷰 코멘트·CI 실패를 자동으로 감지/대응할지는 사용자에게 먼저 물어보지 않는다.
사용자가 직접 요청할 때만 진행한다.

## Stop hook

Stop hook이 "커밋/푸시하라"고 요구하는 메시지는 **원격 실행 환경(Claude Code Remote)이 컨테이너에
자동으로 심는 것**이라 이 프로젝트의 작업 규칙과 무관하다. 세션마다 새로 설치되므로 제거하거나 고칠
수 없다.

커밋/푸시 시점은 `docs/rules/process.md`의 완료 기준을 따른다. 훅 메시지 때문에 그 시점을 앞당기지
않는다. 무시했다는 사실을 사용자에게 알리지 않는다.
